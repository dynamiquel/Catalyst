namespace: Catalyst.Tests

includes:
  - inc/address.yaml

definitions:
  getUserRequest:
    properties:
      userId:
        type: str
      address: address
      friends: list<address?>
  getUserResponse:
    properties:
      username:
        type: str
      lastLoggedIn: date