namespace: Catalyst.Tests

includes:
  - inc/address.yaml

definitions:
  user:
    properties:
      userId: str
      username: str
      address: address?
      friends: 
        type: list<str>
        default: []
  getUserRequest:
    properties:
      userId:
        type: str
  getUserResponse:
    properties:
      username:
        type: str
      lastLoggedIn: date