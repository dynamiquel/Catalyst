# example of a global file, which is used to set default options that propagate
# to all files in the current directory and subdirectories.

unreal:
  prefix: CatalystTest    # example of using an Unreal Engine file and class prefix for all files by default